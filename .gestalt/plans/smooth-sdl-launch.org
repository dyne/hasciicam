#+TITLE: Smooth SDL Launch And Semantic Releases
#+SUBTITLE: Versioned master releases plus first-click SDL window sizing
#+DATE: 2026-05-29
#+KEYWORDS: hasciicam semantic-release sdl launch sizing github-actions

* TODO [#A] Add semantic versioning and semantic-release
:PROPERTIES:
:Effort: 1 day
:Goal: Pushes to master calculate the next SemVer version from conventional commits and publish the existing platform zip artifacts through semantic-release.
:Notes: Start here because release identity should drive artifact naming and GitHub release creation. Use cycjimmy/semantic-release-action@v6.0.0, the latest release checked on 2026-05-29. Keep the existing build/test matrix as the producer of win/mac/linux zip files, but remove the manual sha-tag release creation once semantic-release owns releases.
:END:

** TODO [#A] Replace sha-tag release creation with semantic-release
:PROPERTIES:
:Why: Current master releases are tagged as v<cmake-version>-<short-sha>, which is not SemVer and does not use conventional commits.
:Change: In .github/workflows/ci.yml, keep build-and-test as the required matrix job. Change the release job so it downloads hasciicam-linux.zip, hasciicam-mac.zip, and hasciicam-win.zip, then invokes cycjimmy/semantic-release-action@v6.0.0. Configure env exactly with GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}. Remove the shell gh release create step. Ensure the release job runs only on push to master and only when build-and-test succeeds.
:Tests: Run git diff --check. Validate workflow structure by reviewing the rendered YAML indentation and GitHub expression placement. Push to a test branch or draft PR to confirm PRs run build/test without release.
:Done when: Pushes to master no longer create sha-based releases and the only release-creation path is semantic-release.
:END:

** TODO [#A] Add semantic-release configuration
:PROPERTIES:
:Why: The action needs deterministic release rules, branch selection, release-note generation, and asset upload rules.
:Change: Add a root .releaserc.json. Set branches to ["master"]. Use @semantic-release/commit-analyzer and @semantic-release/release-notes-generator defaults for conventional commits. Add @semantic-release/changelog because it was requested. Configure @semantic-release/github assets for the three downloaded zip files with labels "Linux", "macOS", and "Windows". Decide explicitly whether CHANGELOG.md should be committed: if yes, add @semantic-release/git to extra_plugins and to .releaserc.json; if no, document that @semantic-release/changelog writes the file during the release job but does not persist it in the repository.
:Tests: Run npx semantic-release --dry-run locally if Node is available and credentials are not required for the dry run; otherwise inspect the config against semantic-release plugin syntax. Confirm asset paths match actions/download-artifact output.
:Done when: semantic-release can compute the next version from conventional commits and publish all three platform zips to the release it creates.
:END:

** TODO [#B] Align artifact names and package metadata with release version
:PROPERTIES:
:Why: Release assets should be easy to identify and should not collide across versions.
:Change: Keep uploaded artifact names stable for workflow transfer, but rename final zip files before semantic-release uploads them to include the semantic-release version if the action exposes it in the workflow. If version-aware renaming is awkward inside semantic-release, keep zip names stable and rely on the GitHub release tag for version identity. Do not duplicate version sources in CMake unless semantic-release needs project metadata.
:Tests: Review workflow logs on the first master release to confirm the release tag is SemVer and all three zip assets are attached.
:Done when: Reviewers can tell which version each platform zip belongs to from the GitHub release page.
:END:

* TODO [#A] Introduce screen-aware default launch sizing
:PROPERTIES:
:Effort: 2 days
:Goal: Running hasciicam with no user options opens a smooth SDL live window that fills most of the primary screen without requiring CLI knowledge.
:Notes: The existing size model already maps final window pixels to ascii grid and capture target sizes. Reuse that. Keep explicit user size flags authoritative. Avoid adding dependencies; SDL2 already knows the display bounds when the SDL driver is compiled.
:END:

** TODO [#A] Detect whether the user requested an explicit size or driver
:PROPERTIES:
:Why: Automatic first-click defaults must not override CLI users who pass -s, --pixel-size, --char-size, AA-lib width options, or -O.
:Change: Extend hasciicam_config with flags such as explicit_size and explicit_aadriver. Set them in hasciicam_config_parse when parsing -s, --pixel-size, --char-size, or -O. Keep mode-specific behavior unchanged: HTML -s remains character size, live/text -s remains pixel size.
:Tests: Add app_config or app_size focused C tests if an existing test can cover parser state without spawning the app. Otherwise extend cli_size_semantics.cmake to verify explicit size behavior remains unchanged.
:Done when: Later default logic can reliably tell "no option first-click launch" apart from intentional CLI sizing.
:END:

** TODO [#A] Add a display sizing port with SDL adapter
:PROPERTIES:
:Why: The app needs screen dimensions before choosing capture and ascii sizes, but screen IO should stay isolated from app sizing rules.
:Change: Add a small C port, for example src/display/display_size.h and src/display/display_size_sdl.c, with hasciicam_display_size_detect_primary(int *w, int *h). When SDL is available, initialize the video subsystem if needed and call SDL_GetDisplayUsableBounds(0, ...), falling back to SDL_GetCurrentDisplayMode(0, ...). Return 0 if SDL is unavailable or detection fails. Do not open the main render window in this detector.
:Tests: Add a deterministic unit test for the fallback sizing calculation without needing real SDL. Do not add mandatory CI tests that require a windowed display.
:Done when: The application can ask for primary usable display pixels through one narrow function and can cleanly fall back when unavailable.
:END:

** TODO [#A] Compute a "most of screen" first-launch size
:PROPERTIES:
:Why: The default should feel intentional on first click: large enough to be useful, not fullscreen, and constrained to display bounds.
:Change: Add an app_size helper such as hasciicam_size_build_default_live_plan(metrics, screen_w, screen_h, plan). Use 85% to 90% of usable display width and height, then round down to whole SDL character cells. Preserve aspect ratio by fitting the camera target within that window-derived ascii grid; prefer common capture sizes through existing backend negotiation rather than forcing exact dimensions. Set conservative minimums, for example 640x480 display pixels or 80x25 chars when detection fails.
:Tests: Add C tests in tests/test_app_size.c for 1920x1080, 1366x768, small display fallback, and exact cell rounding. Assert output pixel targets never exceed the chosen screen fraction and ascii sizes are positive.
:Done when: With no size flags, the app produces a predictable preferred ascii grid and requested capture target derived from detected screen size.
:END:

** TODO [#A] Apply default sizing only for live SDL first-click launch
:PROPERTIES:
:Why: Text and HTML output should remain deterministic file modes, and explicit CLI users should keep control.
:Change: In src/hasciicam.c, after parsing config and before opening capture, if mode is LIVE, no explicit size was supplied, and no explicit driver was supplied, set default driver intent to SDL and build the default live size plan from display detection. If SDL detection fails, keep the current auto-driver fallback order and current default dimensions. Do not break synthetic:// smoke tests: when --frames is used with synthetic:// in CI, keep the ability to run headless by allowing -O stdout or existing test options to override SDL.
:Tests: Add a CLI smoke case that passes no size but uses synthetic:// and -O stdout to prove explicit driver still bypasses SDL-first defaults. Add unit coverage for config flags. Manual smoke after build: double-click or run hasciicam with no options on a machine with SDL and a camera.
:Done when: No-option live startup prefers SDL and chooses a screen-fitting size, while all existing explicit options behave as before.
:END:

* TODO [#B] Make SDL window behavior match the chosen size
:PROPERTIES:
:Effort: 1 day
:Goal: The SDL driver opens a polished initial window at the requested character grid, centered or naturally placed by the OS, with stable resizing behavior.
:Notes: Keep changes inside the existing AA-lib SDL driver unless a small helper is clearly cleaner. Do not add a separate output framework.
:END:

** TODO [#B] Teach the SDL driver to use usable display bounds defensively
:PROPERTIES:
:Why: Even if the app computes a good size, window managers can constrain sizes; SDL should avoid opening larger than the usable screen.
:Change: In src/aalib/aasdl.c, clamp initial win_width/win_height to SDL_GetDisplayUsableBounds for the target display when p->recwidth/recheight are large. Recompute d->width/d->height from the actual window size as it already does. Keep SDL_WINDOW_RESIZABLE and avoid fullscreen by default.
:Tests: Build with SDL enabled. Run visual_sdl manually or with opt-in HASCIICAM_ENABLE_VISUAL_TESTS=ON where a display exists.
:Done when: SDL never requests a window larger than the usable display and still reports the actual character grid back to AA-lib.
:END:

** TODO [#B] Preserve driver fallback and diagnostics
:PROPERTIES:
:Why: SDL may be absent, unavailable over SSH, or blocked by OS permissions. First-click should be smooth, but terminal users still need meaningful failures.
:Change: Keep aa_recommendhidisplay("SDL") first. If the no-option path explicitly sets SDL as preferred, decide whether to allow AA-lib fallback to X11/curses/stdout or to fail with a clear SDL message. Prefer fallback for CLI robustness, but log "SDL unavailable, falling back" when not quiet.
:Tests: Configure with HASCIICAM_ENABLE_SDL=OFF and run hasciicam -d synthetic:// --frames 2 -O stdout. Confirm no new SDL dependency is required for headless CI.
:Done when: SDL-first behavior improves desktop launches without making headless or non-SDL builds unusable.
:END:

* TODO [#B] Update tests, docs, and release notes
:PROPERTIES:
:Effort: 1 day
:Goal: The behavior is covered, documented, and visible to users installing release zips.
:Notes: User-facing behavior changes require README and man page updates. Keep generated artifacts out of source tree.
:END:

** TODO [#B] Extend automated tests around sizing and CLI semantics
:PROPERTIES:
:Why: The default path touches startup behavior, screen sizing, and existing size flags.
:Change: Add app_size tests for screen-fit calculations. Add config parser tests if practical. Extend CLI smoke only where it remains deterministic in headless CI; do not make SDL visual tests mandatory.
:Tests: Run cmake -S . -B build -G Ninja -DHASCIICAM_ENABLE_TESTS=ON -DHASCIICAM_ENABLE_SDL=OFF, cmake --build build, and ctest --output-on-failure --test-dir build. For SDL-capable machines, also run the visual SDL smoke manually.
:Done when: Existing tests pass and new sizing tests fail before the implementation but pass after it.
:END:

** TODO [#B] Update README, man page, and AGENTS.md if needed
:PROPERTIES:
:Why: First-click SDL default and semantic-release versioning are user-visible and maintainer-visible changes.
:Change: Update README startup examples to say no-option desktop launch opens SDL at a screen-fitting size when SDL is available. Update doc/hasciicam.1 for default live mode and size behavior. Add a short AGENTS.md note only if the release workflow or SDL sizing conventions become maintainer instructions.
:Tests: Review rendered Markdown/man text manually. Run hasciicam -h and hasciicam -H if help text changes.
:Done when: Users and maintainers can discover the new default without reading source code.
:END:

** TODO [#C] Perform release workflow smoke after merge
:PROPERTIES:
:Why: semantic-release behavior can only be fully proven in GitHub Actions with repository tokens and master push context.
:Change: After implementation PR merges, inspect the master workflow run. Confirm build/test matrix outcome, semantic-release version decision, changelog behavior, release tag, and attached win/mac/linux zip files.
:Tests: GitHub Actions master run is the test. If semantic-release reports "no release published", verify conventional commits since last tag justify that outcome.
:Done when: The first qualifying master push produces one SemVer GitHub Release with all requested platform zips.
:END:
